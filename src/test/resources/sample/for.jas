list = [5, 4, 3, 2, 1]

for item in list
    s = ""
    for i in list
        if i % 2 == 0 then begin
            s += (item + " * " + i + " = " + (item * i)) + ", "
        end
    end
    print s
end
    print "---"
